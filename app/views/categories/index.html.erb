<div class="img-hero">
    <div class="big-title">
      <h1>Find real quality art pieces made by professionals.</h1>
      <h3>Discover unique work, created by originial artists all around the world.</h3>
    </div>

    <div class="container form-hero">
      <form class="form-inline">
        <span class="form-group">
          <input type="text" class="form-control form-case col-md-3" placeholder="fucking cool poster">
        </span>
        <span>
        <button type="submit" class="btn btn-default btn-hero">LET'S FIND IT!</button>
        </span>
      </form>
    </div>

</div>
<% @categories.each do |category| %>
<%= category.name %>
<% end %>
